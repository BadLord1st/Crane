# QA Output

## Commands run

- `cargo test -p crane-core gemma4::modeling::tests`
- `cargo test -p crane-core gemma4`
- `cargo test -p crane-oai model_factory`
- `cargo build -p crane-oai --release`

## Results

- `cargo test -p crane-core gemma4::modeling::tests` → passed
  - `3 passed; 0 failed; 0 ignored; 0 measured; 12 filtered out`
- `cargo test -p crane-core gemma4` → passed
  - same 3 Gemma4 modeling tests executed successfully
- `cargo test -p crane-oai model_factory` → passed
  - `19 passed; 0 failed; 0 ignored; 0 measured; 113 filtered out`
- `cargo build -p crane-oai --release` → passed
- Warnings remain in unrelated areas of `crane-core`, but no new verification failure was observed for this slice.

## AC coverage verdict

- `AC-1` — partially evidenced
  - Proven at unit level for the identified root cause: missing `v_proj` no longer blocks attention construction when `attention_k_eq_v=true`.
  - Not yet proven with a full real-checkpoint startup test.
- `AC-2` — partially evidenced
  - Proven at unit level that missing `v_proj` still fails when `attention_k_eq_v=false`.
  - Diagnostic startup error behavior for other incompatibility classes is not fully exercised here.
- `AC-3` — partially evidenced
  - The path where `v_proj` exists remains supported by construction and all relevant tests/builds passed.
  - No real previously-working Gemma4 checkpoint was reloaded end-to-end in this QA pass.
- `AC-4` — not directly evidenced in this slice
  - Missing-file and broader bounded-startup error cases were not exercised.

## Gaps / risks

- No end-to-end startup against the actual PVC-backed 26B Gemma4 checkpoint yet.
- No live cluster smoke check yet because deployment remains at `0` replicas.
- Coverage is intentionally narrow around the confirmed root cause (`attention_k_eq_v` + absent `v_proj`).
- Other Gemma4 incompatibility surfaces from the spec remain only specified, not fully tested.

## Recommendation

- Accept T-0001 as code-complete for the identified loader defect.
- Before closing T-0001 fully, run one real startup verification against the 26B PVC model path.
- Then proceed with T-0002: build CUDA image on `my69`, push, update deployment image, scale to 1 replica, and verify `/health` plus pod logs.
